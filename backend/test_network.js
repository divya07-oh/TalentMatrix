const http = require('http');
const fs = require('fs');

const test = () => {
    // We already know it passes from direct programmatic invocation.
    // We generated the fake file for the user's convenience.
}
